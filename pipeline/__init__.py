# pipeline — the voice-pipeline package: stt (VAD/denoise/whisper), routing
# (language/dialect detection + text-acceptance policy), llm (Ollama client,
# prompt construction), arabic_numbers (dialect number verbalization),
# tts_omnivoice_v1 (Fusha/Najdi/English TTS) and tts_voicetut_v1 (Egyptian TTS).
# server.py at the project root is the entry point that orchestrates these.
